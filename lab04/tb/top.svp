module top;
import fifomult_tb_pkg::*;

    switch_bfm bfm();

    simple_switch_uart dut (
    .clk    (bfm.clk),
    .rst_n  (bfm.rst_n),
    .prog   (bfm.prog),
    .sin    (bfm.sin),
    .sout0  (bfm.sout0),
    .sout1  (bfm.sout1)
    );

    testbench testbench_h;
    
    initial begin
        testbench_h = new(bfm);
        testbench_h.execute();
        //$finish;
    end

endmodule : top